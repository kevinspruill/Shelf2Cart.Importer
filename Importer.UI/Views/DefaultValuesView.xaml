<UserControl
    x:Class="Importer.UI.Views.DefaultValuesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Margin="0,4" Text="Default Category Number" />
                <TextBox
                    Grid.Column="1"
                    Margin="5,4"
                    Text="{Binding DefCatNum}" />

                <TextBlock
                    Grid.Row="1"
                    Margin="0,4"
                    Text="Default Price" />
                <TextBox
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="5,4"
                    Text="{Binding DefPrice}" />

                <TextBlock
                    Grid.Row="2"
                    Margin="0,4"
                    Text="Default Shelf Life" />
                <TextBox
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="5,4"
                    Text="{Binding DefShelfLife}" />

                <TextBlock
                    Grid.Row="3"
                    Margin="0,4"
                    Text="Default Net Weight" />
                <TextBox
                    Grid.Row="3"
                    Grid.Column="1"
                    Margin="5,4"
                    Text="{Binding DefNetWt}" />

                <TextBlock
                    Grid.Row="4"
                    Margin="0,4"
                    Text="Default Barcode Value" />
                <TextBox
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="5,4"
                    Text="{Binding DefBarcodeVal}" />

                <TextBlock
                    Grid.Row="5"
                    Margin="0,4"
                    Text="Default Bar Type" />
                <TextBox
                    Grid.Row="5"
                    Grid.Column="1"
                    Margin="5,4"
                    Text="{Binding DefBarType}" />

                <TextBlock
                    Grid.Row="6"
                    Margin="0,4"
                    Text="Default Label Name" />
                <TextBox
                    Grid.Row="6"
                    Grid.Column="1"
                    Margin="5,4"
                    Text="{Binding DefLabelName}" />
            </Grid>
        </ScrollViewer>

        <StackPanel
            Grid.Row="1"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="100"
                Height="35"
                Margin="0,0,8,0"
                Command="{Binding ReloadCommand}"
                Content="Reload" />
            <Button
                Width="100"
                Command="{Binding SaveCommand}"
                Content="Save" />
        </StackPanel>
    </Grid>
</UserControl>
