<UserControl x:Class="Importer.UI.Views.AdminSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="650"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Margin="0,4" Text="Admin Console Processing Folder" />
            <TextBox Grid.Column="1" Margin="5,4" Text="{Binding AdminConsoleProcessingFolder}" />
            <Button Grid.Column="2" Margin="5,4" Content="Browse" Width="70" Command="{Binding BrowseProcessingFolderCommand}" />

            <TextBlock Grid.Row="1" Margin="0,4" Text="Admin Console File Name" />
            <TextBox Grid.Row="1" Grid.Column="1" Margin="5,4" Text="{Binding AdminConsoleFileName}" />
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Width="100" Margin="0,0,8,0" Command="{Binding ReloadCommand}" Content="Reload" />
            <Button Width="100" Command="{Binding SaveCommand}" Content="Save" />
        </StackPanel>
    </Grid>
</UserControl>
